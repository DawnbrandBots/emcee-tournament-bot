describe("Remove host", function () {
	it("Normal functionality");
	it("User is not a host");
	it("User is the last host");
	it("Missing user mention");
	it("Missing tournament ID");
	it("Invalid tournament ID");
	it("Missing all args");
	it("Caller not host");
});
