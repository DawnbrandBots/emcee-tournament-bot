describe("Add announcement channel", function () {
	it("All parameters");
	it("Default to current channel");
	it("Default to public and current channel");
	it("Channel is already a public announcement channel");
	it("Channel is already a public channel, but we can add private");
	it("Missing tournament ID");
	it("Invalid tournament ID");
	it("Caller not host");
});
