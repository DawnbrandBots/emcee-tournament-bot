describe("Remove announcement channel", function () {
	it("All parameters");
	it("Default to current channel");
	it("Default to public and current channel");
	it("Channel is not a public announcement channel");
	it("Channel is a public channel, but we can't remove private");
	it("Missing tournament ID");
	it("Invalid tournament ID");
	it("Caller not host");
});
